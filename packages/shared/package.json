{
  "name": "@valorasec/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, constants, and utilities for ValoraSec",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
